--- ./llvm/Makefile.orig	2014-07-16 21:43:17.000000000 +0200
+++ ./llvm/Makefile	2014-07-28 20:06:44.000000000 +0200
@@ -2,6 +2,9 @@
 
 PKG_CONFIG_CFLAGS:=$(shell (pkg-config --version >/dev/null 2>&1) && pkg-config --cflags bdw-gc)
 CFLAGS:=-Wextra -fPIC -Wno-unused-parameter $(PKG_CONFIG_CFLAGS) $(CFLAGS)
+ifneq ($(GMP_INCLUDE_DIR),)
+	CFLAGS += -isystem $(GMP_INCLUDE_DIR)
+endif
 SOURCES=defs.c
 OBJECTS=$(SOURCES:.c=.o)
 LIB=libidris_rts.a
